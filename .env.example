PORT=3000
JWT_SECRET=replace_with_dev_secret

# Database (optional). If not using DB, set USE_DB=false
USE_DB=true
DB_HOST=localhost
DB_PORT=5432
DB_USER=enyalis
DB_PASSWORD=change_this
DB_NAME=enyalis_db

# Require DB presence (production only typically)
REQUIRE_DB=false

# Additional placeholders for future integrations
# MINIO_ENDPOINT=
# MINIO_PORT=9000
# MINIO_ACCESS_KEY=
# MINIO_SECRET_KEY=
# MINIO_BUCKET=enyalis-attachments
# MINIO_USE_SSL=false
