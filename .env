PORT=3000
JWT_SECRET=supersecret123dev

# --- Database (Development) ---
# Toggle DB usage (set to false for pure in-memory testing)
USE_DB=true
DB_HOST=localhost
DB_PORT=5432
DB_USER=enyalis
DB_PASSWORD=enyalis_password
DB_NAME=enyalis_db

# Fail fast if DB unavailable (leave false for dev convenience)
REQUIRE_DB=false

# Future: add REDIS_HOST, MINIO_ENDPOINT, etc. here
